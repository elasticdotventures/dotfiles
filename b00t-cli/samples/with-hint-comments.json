// Language Server Protocol via Docker
{
  "mcpServers": {
    "lsp": {
      // Provides LSP functionality through Docker container
      "command": "docker",
      "args": ["run", "-i", "--rm", "docker.io/jonrad/lsp-mcp:0.3.1"]
    }
  }
}