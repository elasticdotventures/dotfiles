# Claude CLI container with Docker CLI for building b00t containers
# Extends node:20-bookworm with Docker CLI to enable Docker-in-Docker builds

FROM node:20-bookworm

# Install Docker CLI (for building b00t containers via socket mount)
RUN apt-get update && apt-get install -y \
    ca-certificates \
    curl \
    gnupg \
    && install -m 0755 -d /etc/apt/keyrings \
    && curl -fsSL https://download.docker.com/linux/debian/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg \
    && chmod a+r /etc/apt/keyrings/docker.gpg \
    && echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/debian bookworm stable" \
      | tee /etc/apt/sources.list.d/docker.list > /dev/null \
    && apt-get update \
    && apt-get install -y docker-ce-cli docker-compose-plugin \
    && rm -rf /var/lib/apt/lists/*

# Verify Docker CLI installed
RUN docker --version

# Keep node user and environment
USER node
WORKDIR /home/node

# Default: run npx claude
CMD ["npx", "--yes", "@anthropic-ai/claude"]
